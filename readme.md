#AWS Redshift Manager

###Dependencies
pip install pyyaml
pip install redshift_connector
pip install pandas

#AWS Athena Manager

###Dependencies
pip install boto3
pip install awswrangler
pip install pyyaml
